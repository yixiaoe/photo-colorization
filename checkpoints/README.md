# checkpoints

This directory stores model weights generated during training.

Expected Phase 1 layout:

```text
checkpoints/
└── zhang2016/
    ├── latest_net_G.pth
    └── 1_net_G.pth
```

Checkpoint files are ignored by Git.

